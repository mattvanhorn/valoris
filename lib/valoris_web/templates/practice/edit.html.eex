<h1>Edit Practice</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.goal_practice_path(@conn, :update, @practice.goal, @practice)) %>

<span><%= link "Back", to: Routes.landing_page_path(@conn, :index) %></span>
