<h1>New Action</h1>

<p><%= if @goal, do: @goal.name %></p>
<%= render "form.html", Map.put(assigns, :action, Routes.goal_action_path(@conn, :create, @goal)) %>

<span><%= link "Back", to: Routes.goal_action_path(@conn, :create, @goal) %></span>
